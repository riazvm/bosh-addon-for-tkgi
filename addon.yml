---
name:  tkgi-update-ringbuffer

releases:
- name:  tkgi-update-ringbuffer
  version: 0+dev.9

addons:
  - name:  tkgi-update-ringbuffer
    jobs:
    - name: update-ringbuffer
      release:  tkgi-update-ringbuffer
    include: 
      jobs:
      - name: kubelet
        release: kubo
